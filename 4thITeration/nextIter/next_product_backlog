*Add reference options ... like what happens if some referencing rows get deleted

[CONSTRAINT [symbol]] FOREIGN KEY
    [index_name] (col_name, ...)
    REFERENCES tbl_name (col_name,...)
    [ON DELETE reference_option]
    [ON UPDATE reference_option]

reference_option:
    RESTRICT | CASCADE | SET NULL | NO ACTION | SET DEFAULT

*Add new views... you can do basically whatever you wanna.

*Add new update policies
https://www.fromdual.com/mysql-materialized-views

*Indeces have tons of tuning parameters! Check them out:
https://dev.mysql.com/doc/refman/8.0/en/create-index.html


