#ONLY \n-ERASABLE COMMENTS 
CREATE VIEW Info(first_name, last_name, phone_number, email, general, DOB)
CREATE TABLE SaleDao(Price) #REAL TABLE
CREATE VIEW Point(Count(*)) #PUT A FAKE AUTOINCREMENT IF YOU WANNA
CREATE JAVA TheShittyTable

#--------star schema implementation stops here

#SnowFlakes, a recursive solution
#CREATE JAVA is a ... Leave the world as it is, somembodyelse's gonna do that for yoau
CREATE PROGRAM C/C++ of CHASHING for f in F1
CREATE PROGRAM JAVA (probably a wrapper / framework) for f in F2
# Java module in the python script is gonna immplement


################### MY sql script

CREATE TABLE  IF NOT EXISTS SaleDao(
	price decimal(4,2),
)


CREATE TABLE customer IF NOT EXISTS(
	##customer
	marital_status varchar(8), #Married, Single, Divorced, Widowed
	nationality varchar(14),
	card_id int(8),
		loyalty varchar(6),
			points int(10),
	country varchar(20),
		city varchar(22),
			street varchar(40),
				building int(2),
	SSN int(9), # it would be cool to cache them
		first_name varchar(20),
		last_name varchar(20),
		phone varchar(20),
		email varchar(40),
		gender char(1), # M:male, F:famale, U:unknown
		DOB date
)

CREATE TABLE details IF NOT EXISTS(
	quantity int(8),
	discount decimal(2,2),

)

CREATE TABLE employee IF NOT EXISTS(
	first_name varchar(20),
	second_name varchar(20),
	last_name varchar(20)
	#branch key 
)



CREATE TABLE info IF NOT EXISTS(

)
